1.对于基本数据类型和引用型数据类型来说，运算符“==”在比较两个变量是否相同时，含义有差别因为：
对于基本数据类型（如int、double、boolean等），"=="比较的是它们的值是否相等。如果两个基本类型的变量存储的数值相同，则"=="返回true；否则返回false。
对于引用型数据类型（如对象），"=="比较的是对象的引用（内存地址）是否相同，而不是对象的内容是否相同。如果两个引用型变量指向的是相同的对象（即指向同一个内存地址），则"=="返回true；如果指向不同的对象，则返回false。
